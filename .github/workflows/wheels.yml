# (C) 2025: Hans Georg Schaathun <georg+professor@schaathun.net> 

name: cibuildwheel

description: |
  Build python wheels for deployment to the PyPI server.
  This is the most critical workflow in the project.
  As of August 2025 it works with the architectures present.

  Building on Windows is erratic.  It succeeded once on 4 Aug'25,
  but subsequent runs of the same version on the same day failed.

on: 
   push:
       branches: [ "build/pypi*", "refactor/workflow*" ]
   workflow_call:
   workflow_dispatch:

jobs:
  build_wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    # Windows wheels are still being stabilised.  Mark only the Windows leg as
    # non-blocking so a Windows failure does not fail this (reusable) workflow,
    # which would otherwise skip the dependent `upload` job in pypideploy.yml /
    # pypitest.yml and block publication of the working Linux/macOS wheels.
    # Remove this once Windows builds are reliably green to make it a hard gate.
    continue-on-error: ${{ matrix.os == 'windows-latest' }}
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, macos-latest, windows-latest ]
# Not enabled yet (unproven / extra cost): ubuntu-24.04-arm, windows-11-arm, macos-13

    steps:
      - uses: actions/checkout@v4

      # Used to host cibuildwheel
      - uses: actions/setup-python@v5
        with:
           python-version: '3.13'

      - name: Upgrade pip
        run: |
           python --version
           python -m pip install --upgrade pip

      - name: Install cibuildwheel
        # run: python -m pip install cibuildwheel==3.4.1
        run: python -m pip install cibuildwheel==4.0.0rc1

      - name: Sleep (1)
        run: sleep 60

      - name: Build wheels
        run: python -m cibuildwheel --output-dir wheelhouse

      - name: Sleep (2)
        run: sleep 60

      - uses: actions/upload-artifact@v4
        with:
          name: cibw-wheels-${{ matrix.os }}-${{ strategy.job-index }}
          path: ./wheelhouse/*.whl

  build_sdist:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Project
        uses: ./.github/actions/build
        with:
           python-version: '3.13'
           compiler-version: 13

      - name: Build sdist
        run: pipx run build --sdist

      - uses: actions/upload-artifact@v4
        with:
          name: cibw-sdist
          path: dist/*.tar.gz

